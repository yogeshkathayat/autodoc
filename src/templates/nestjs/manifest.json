{
  "$schema": "https://unpkg.com/autodocai/schema/manifest.schema.json",
  "version": "1.0",
  "project": "{{PROJECT_NAME}}",
  "framework": "nestjs",
  "adapter": "claude-code",
  "description": "Auto-generated feature-based documentation manifest for NestJS project",
  "mappings": [
    {
      "id": "architecture-overview",
      "doc": "docs/architecture-overview.md",
      "watches": ["src/**/*.module.ts", "src/main.ts"],
      "purpose": "High-level system architecture: module dependency graph with Mermaid diagrams, global middleware/guards/interceptors, configuration, database connections, external service integrations.",
      "strategy": "rewrite"
    }
  ],
  "ignore": ["**/*.spec.ts", "**/*.test.ts", "**/node_modules/**", "dist/**"]
}
